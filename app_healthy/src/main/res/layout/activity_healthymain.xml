<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ignore="MissingDefaultResource">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <View
        android:id="@+id/stu"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        app:status="@{null}" />

    <FrameLayout
        android:id="@+id/fl_home_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.zkwl.app_healthy.bottombarlayout.BottomBarLayout
        android:id="@+id/bbl_home_tab"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="#ffffff"
        android:gravity="center"
        android:orientation="horizontal">

        <com.zkwl.app_healthy.bottombarlayout.BottomBarItem
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            app:iconNormal="@mipmap/ic_tab_home_n"
            app:iconSelected="@mipmap/ic_tab_home"
            app:itemMarginTop="5dp"
            app:itemText="首页"
            app:itemTextSize="12sp"
            app:openTouchBg="true"
            app:textColorNormal="@color/color_999"
            app:textColorSelected="@color/colorPrimary"
            app:touchDrawable="@drawable/bot_selector_bg" />





    </com.zkwl.app_healthy.bottombarlayout.BottomBarLayout>
</LinearLayout>
</layout>